<div class="display-container">
  <div class="display-container-title">
    <h2>Náhled displeje</h2>
  </div>

  <div class="display">
    <ul class="display-events">
      <% if (calendar.events.length > 0) { %>
        <% calendar.events.forEach(event => { %>
          <li class="display-events__item">
            <strong>
              <%= event.summary %>
            </strong>
            <time datetime="<%= event.start %>">
              <%= event.start %>
            </time>
          </li>
        <% }) %>
      <% } else { %>
        <li>
          Žádné nadcházející události
        </li>
      <% } %>
    </ul>

    <span class="display-weather">
      <%= weather.current?.temperature_2m || "-" %>&nbsp;<%= weather.current_units?.temperature_2m || "-" %>
    </span>

    <span class="display-timestamp">
      <%= timestamp %>
    </span>
  </div>
</div>
